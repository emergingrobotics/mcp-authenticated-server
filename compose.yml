# MCP server container.
# The database and embedding server are external dependencies
# and must be running before starting this container.
# See README.md for prerequisites.

services:
  server:
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "9090:9090"
    env_file:
      - .envrc
    restart: unless-stopped
